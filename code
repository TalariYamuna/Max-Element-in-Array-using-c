#include <stdio.h>
#include <stdlib.h>
int main() {  
   int n,i,max;
   long long a[1000];
   scanf("%d",&n);
   for(i=0;i<n;i++)
   scanf("%lld",&a[i]);
   max=a[0];
   for(i=1;i<n;i++)
   {
       if(a[i]>max)
       max=a[i];
   }
   printf("%d",max);
    return 0;
}
